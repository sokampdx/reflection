Lesson 2


1.
Staging area put file that is ready for the next commit. working directory is only a place holder for all the files. The value of staging area is to let user put more than one file into a single commit. 

2.
By only adding the files that fit into one logical changes in one comit. Use git diff and git diff --staged to make sure the changes are logical.

3.
Trying different approach on the same applications, having a divergence of different implementation.

git log --graph --oneline master coins.

4.
It shows how each branch connect to each other.

5.
Auto-merging game.js
CONFLICT (content): Merge conflict in game.js
Automatic merge failed; fix conflicts and then commit the result.

6.
auto merge add line that are edit at different location. manual merge add line at the same location






